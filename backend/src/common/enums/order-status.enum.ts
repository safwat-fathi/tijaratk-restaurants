export enum OrderStatus {
  DRAFT = 'draft',
  CONFIRMED = 'confirmed',
  OUT_FOR_DELIVERY = 'out_for_delivery',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  REJECTED_BY_CUSTOMER = 'rejected_by_customer',
}
